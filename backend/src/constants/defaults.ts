// Valori default (limită, offset, sortare, monedă etc.)

export const PAGINATION = {
  DEFAULT_LIMIT: 20, // Numărul implicit de rezultate pe pagină
  DEFAULT_OFFSET: 0, // Offset implicit pentru paginare
  DEFAULT_SORT: 'date', // Sortare implicită după dată
};

export const APP_DEFAULTS = {
  CURRENCY: 'RON', // Moneda implicită
  DATE_FORMAT: 'YYYY-MM-DD', // Format de dată implicit
};
